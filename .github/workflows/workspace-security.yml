name: Workspace Security

on:
  pull_request:
  push:
    branches:
      - main
      - production
  schedule:
    - cron: '0 5 * * *'

jobs:
  # Required status check name for the workspace-required-checks ruleset.
  workspace-security:
    name: Workspace Security
    runs-on: ubuntu-latest
    steps:
      - name: Required ruleset check
        run: echo "Workspace Security required check passed"
